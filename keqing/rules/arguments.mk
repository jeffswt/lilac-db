# List of binary programs referenced by the make script.

# GNU coreutils
BIN_BASH        = bash
BIN_BASENAME    = basename
BIN_CAT         = cat
BIN_CP          = cp
BIN_ECHO        = echo
BIN_FIND        = find
BIN_GIT         = git
BIN_MAKE        = make
BIN_MKDIR       = mkdir
BIN_READLINK    = readlink
BIN_RM          = rm
BIN_TAR         = tar
BIN_TEE         = tee
BIN_TOUCH       = touch

# Shell binaries
BIN_CURL        = curl
BIN_PERL        = perl
BIN_PYTHON3     = python3

# Rust toolchain
BIN_CARGO       = cargo
BIN_RUSTC       = rustc
BIN_RUSTUP      = rustup
